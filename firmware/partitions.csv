# Single-app partition table for the ESP32-C3 (4 MB flash). InkCards does not
# use OTA, so the whole app region is one factory partition.
# Name,     Type, SubType,  Offset,   Size
nvs,        data, nvs,      0x9000,   0x6000,
phy_init,   data, phy,      0xf000,   0x1000,
factory,    app,  factory,  0x10000,  0x3F0000,
